# Trendova Telegram Bot

بوت يستقبل العملاء المحتملين ويفلترهم ويحوّل الجاد للمالك.

## Environment

- `TELEGRAM_BOT_TOKEN` — من @BotFather
- `ANTHROPIC_API_KEY` — Claude API
- `OWNER_CHAT_ID` — (اختياري) Telegram chat ID لإبراهيم

## Deploy

على Railway: service جديد من نفس repo، root = `bot/`
